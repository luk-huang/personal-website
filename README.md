# Luke J. Huang Personal Website

A static personal website in the minimal academic style of `seohong.me`.

Open `index.html` directly in a browser, or serve the folder with any static
server. The resume is copied to `assets/Luke_Huang_Resume.pdf`; replace
`assets/profile-placeholder.svg` with a headshot when one is available.

Blog posts live in `blog.html` and the `blog/` directory.
